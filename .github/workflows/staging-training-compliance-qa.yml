name: Staging Training Compliance QA

on:
  workflow_dispatch:
    inputs:
      confirmation:
        description: 'Type AIRTRUST_STAGING_TRAINING_COMPLIANCE_QA to continue'
        required: true
        type: string
      expected_deployed_sha:
        description: 'Exact Worker/frontend SHA currently deployed to staging'
        required: true
        type: string

concurrency:
  group: staging-training-compliance-qa
  cancel-in-progress: false

permissions:
  contents: read
  checks: read
  statuses: read

env:
  NODE_VERSION: '24'
  STAGING_API_BASE_URL: https://airtrust-api-staging.airtrust.workers.dev
  STAGING_FRONTEND_BASE_URL: https://staging.airtrust.pages.dev
  STAGING_D1_NAME: airtrust-db-staging-baseline-20260701
  QA_COMPLIANCE_CODE: QA-COMP-${{ github.run_id }}
  QA_COMPLIANCE_MARKER: QA_ONLY_TRAINING_COMPLIANCE_${{ github.run_id }}

jobs:
  guard:
    name: Training Compliance QA Guard
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
      - name: Validate trusted staging provenance
        shell: bash
        env:
          CONFIRMATION: ${{ inputs.confirmation }}
          EXPECTED_DEPLOYED_SHA: ${{ inputs.expected_deployed_sha }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          RELEASE_SHA: ${{ inputs.expected_deployed_sha }}
        run: |
          set -euo pipefail
          [[ "$GITHUB_REF" == 'refs/heads/main' ]] || { echo MAIN_ONLY >&2; exit 1; }
          [[ "$CONFIRMATION" == 'AIRTRUST_STAGING_TRAINING_COMPLIANCE_QA' ]] || { echo CONFIRMATION_REJECTED >&2; exit 1; }
          [[ "$EXPECTED_DEPLOYED_SHA" =~ ^[0-9a-fA-F]{40}$ ]] || { echo EXPECTED_SHA_INVALID >&2; exit 1; }
          git cat-file -e "$EXPECTED_DEPLOYED_SHA^{commit}"
          git merge-base --is-ancestor "$EXPECTED_DEPLOYED_SHA" HEAD
          node scripts/ci/verify-release-gates.mjs
          node --input-type=module <<'NODE'
          const expected=String(process.env.EXPECTED_DEPLOYED_SHA).toLowerCase();
          const r=await fetch(`${process.env.STAGING_API_BASE_URL}/api/version`); if(!r.ok) throw new Error(`VERSION_HTTP_${r.status}`);
          const j=await r.json(); const sha=String(j?.data?.sourceSha||j?.sourceSha||'').toLowerCase();
          if(sha!==expected) throw new Error(`STAGING_WORKER_SHA_MISMATCH:${sha||'missing'}`);
          const {assertLiveFrontendShaFromOrigin}=await import('./e2e/lib/live-sha-guard.mjs');
          await assertLiveFrontendShaFromOrigin(process.env.STAGING_FRONTEND_BASE_URL,expected.slice(0,7));
          console.log(`TRAINING_COMPLIANCE_PROVENANCE_OK=${expected}`);
          NODE

  d1_fixture:
    name: D1 Seed Synthetic Never-Realized Requirement
    needs: guard
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: staging
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: npm --prefix worker-airtrust ci
      - name: Validate compliance schema postconditions and seed isolated QA fixture
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_D1_MIGRATION_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CONFIRM_STAGING_COMPLIANCE_QA: AIRTRUST_STAGING_TRAINING_COMPLIANCE_QA_SEED
        run: |
          set -euo pipefail
          bash scripts/staging/validate-0491-postconditions.sh --target="$STAGING_D1_NAME"
          bash scripts/staging/validate-0492-postconditions.sh --target="$STAGING_D1_NAME"
          bash scripts/staging/validate-0494-postconditions.sh --target="$STAGING_D1_NAME"
          bash scripts/staging/validate-0508-postconditions.sh --target="$STAGING_D1_NAME"
          node scripts/staging/seed-qa-training-compliance.mjs --code="$QA_COMPLIANCE_CODE" --marker="$QA_COMPLIANCE_MARKER"
          node scripts/staging/seed-qa-training-compliance.mjs --apply --code="$QA_COMPLIANCE_CODE" --marker="$QA_COMPLIANCE_MARKER"

  browser:
    name: Authenticated Read-Only Compliance UI/API QA
    needs: [guard, d1_fixture]
    runs-on: ubuntu-latest
    environment: staging
    timeout-minutes: 30
    env:
      RELEASE_SHA: ${{ inputs.expected_deployed_sha }}
      AUDIT_PROFILE: training-compliance
      E2E_EMAIL: ${{ secrets.STAGING_SMOKE_EMAIL }}
      E2E_PASSWORD: ${{ secrets.STAGING_SMOKE_PASSWORD }}
      QA_ADMIN_EMAIL: ${{ secrets.QA_EXAMINER_ADMIN_EMAIL }}
      QA_ADMIN_PASSWORD: ${{ secrets.QA_EXAMINER_ADMIN_PASSWORD }}
      BASE_URL: https://staging.airtrust.pages.dev
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - name: Run governed read-only training compliance browser QA
        run: npx playwright test --config=e2e/frontend-pr-ui-qa.config.ts

  d1_cleanup:
    name: D1 Cleanup Synthetic Compliance Fixture
    needs: [d1_fixture, browser]
    if: always() && needs.d1_fixture.result != 'skipped'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: staging
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: npm --prefix worker-airtrust ci
      - name: Roll back only this run's synthetic fixture
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_D1_MIGRATION_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CONFIRM_STAGING_COMPLIANCE_QA: AIRTRUST_STAGING_TRAINING_COMPLIANCE_QA_SEED
        run: node scripts/staging/seed-qa-training-compliance.mjs --rollback --apply --code="$QA_COMPLIANCE_CODE" --marker="$QA_COMPLIANCE_MARKER"

  summary:
    name: Training Compliance QA Summary
    needs: [guard, d1_fixture, browser, d1_cleanup]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Publish result
        env:
          EXPECTED_DEPLOYED_SHA: ${{ inputs.expected_deployed_sha }}
        run: |
          {
            echo '## Staging Training Compliance QA'
            echo ''
            echo "- deployed SHA: \`$EXPECTED_DEPLOYED_SHA\`"
            echo '- Schemas 0491 + 0492 + 0494 + 0508 postconditions: `${{ needs.d1_fixture.result }}`'
            echo '- Synthetic never-realized case: `${{ needs.browser.result }}`'
            echo '- Synthetic cleanup: `${{ needs.d1_cleanup.result }}`'
            echo '- Production action: `none`'
          } >> "$GITHUB_STEP_SUMMARY"
          [[ '${{ needs.guard.result }}' == success && '${{ needs.d1_fixture.result }}' == success && '${{ needs.browser.result }}' == success && '${{ needs.d1_cleanup.result }}' == success ]]
