{
  "version": 1,
  "purpose": "P1-05 CI schema bootstrap provenance and staleness contract",
  "base_schema": "scripts/schema-local.sql",
  "canonical_bootstrap_command": "npm run setup:lms:local:reset",
  "bootstrap_script": "scripts/setup-local-lms-smoke-db.sh",
  "reviewed_through_schema_v2_change": "0487_qualificacoes_renovacoes.sql",
  "critical_tables": [
    "token_blocklist",
    "audit_logs",
    "lms_cursos",
    "lms_matriculas",
    "lms_progresso_scorm",
    "qualificacoes_tipos",
    "qualificacoes_historico",
    "qualificacoes_categorias",
    "qualificacoes_formatos",
    "qualificacoes_tipos_setores",
    "lms_cursos_setores",
    "lms_scorm_package_versions",
    "lms_scorm_package_audit_log",
    "lms_completion_diagnostics_snapshots"
  ],
  "official_consumers": [
    ".github/workflows/heavy-ci.yml",
    ".github/workflows/manual-required-checks.yml",
    "cloudbuild.ci.yaml"
  ],
  "policy": [
    "Official CI consumers must call the canonical bootstrap command instead of replaying arbitrary migrations.",
    "Every migration referenced by the bootstrap must exist in worker-airtrust/migrations.",
    "A Schema V2 change newer than reviewed_through_schema_v2_change that targets a critical table fails closed until this contract/bootstrap is reviewed.",
    "No remote D1 target is permitted by this guard or bootstrap."
  ]
}
