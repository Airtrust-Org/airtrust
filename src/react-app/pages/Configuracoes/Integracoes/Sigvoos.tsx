/**
 * Canonical entrypoint for the active SIGVOOS integration.
 *
 * The implementation remains temporarily in EdApp.tsx only to preserve
 * compatibility with historical imports while callers migrate to this name.
 */
export { default } from './EdApp';
